# Soluciona IA - Staging Docker Compose Example
# Copy this file to docker-compose.staging.yml and configure via .env.staging

version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: soluciona-postgres-staging
    environment:
      POSTGRES_DB: ${DB_NAME:-soluciona_ia_staging}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    volumes:
      - postgres_staging_data:/var/lib/postgresql/data
      - ./init-scripts:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-soluciona_ia_staging}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - soluciona-staging-network

  redis:
    image: redis:7-alpine
    container_name: soluciona-redis-staging
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_staging_data:/data
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - soluciona-staging-network

  minio:
    image: minio/minio:latest
    container_name: soluciona-minio-staging
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY}
    volumes:
      - minio_staging_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - soluciona-staging-network

  keycloak:
    image: quay.io/keycloak/keycloak:24.0
    container_name: soluciona-keycloak-staging
    command: start-dev --import-realm
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/${DB_NAME:-soluciona_ia_staging}
      KC_DB_USERNAME: postgres
      KC_DB_PASSWORD: ${DB_PASS}
      KC_HOSTNAME: ${KEYCLOAK_HOSTNAME:-staging.soluciona.ai}
      KC_HTTP_ENABLED: "true"
      KC_HTTP_PORT: "8080"
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
    volumes:
      - ./keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health/ready"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - soluciona-staging-network

  app-staging:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: soluciona-app-staging
    env_file:
      - .env.staging
    environment:
      NODE_ENV: staging
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-soluciona_ia_staging}
      DB_USER: postgres
      DB_PASS: ${DB_PASS}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY}
      JAEGER_ENDPOINT: jaeger:4317
      OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo:4318
      OTEL_EXPORTER_OTLP_TRACES_ENDPOINT: http://tempo:4318/v1/traces
      OTEL_EXPORTER_OTLP_LOGS_ENDPOINT: http://loki:3100/loki/api/v1/push
    volumes:
      - ./logs:/app/logs
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
      keycloak:
        condition: service_healthy
    networks:
      - soluciona-staging-network
    deploy:
      replicas: 2
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M
          cpus: '0.5'
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
    networks:
      - soluciona-staging-network

  jaeger:
    image: jaegertracing/all-in-one:1.53
    container_name: soluciona-jaeger-staging
    environment:
      COLLECTOR_OTLP_ENABLED: true
      SPAN_STORAGE_TYPE: badger
      BADGER_EPHEMERAL: false
    ports:
      - "16686:16686"
      - "6831:6831/udp"
      - "14268:14268"
      - "4317:4317"
      - "4318:4318"
    volumes:
      - jaeger_staging_data:/var/lib/jaeger
    networks:
      - soluciona-staging-network

  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: soluciona-prometheus-staging
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-lifecycle'
      - '--web.enable-admin-api'
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_staging_data:/prometheus
    ports:
      - "9090:9090"
    networks:
      - soluciona-staging-network

  grafana:
    image: grafana/grafana:10.2.0
    container_name: soluciona-grafana-staging
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD}
      GF_INSTALL_PLUGINS: grafana-piechart-panel
      GF_SERVER_ROOT_URL: https://grafana.staging.soluciona.ai
      GF_SERVER_DOMAIN: grafana.staging.soluciona.ai
      GF_SERVER_ENFORCE_DOMAIN: "true"
      GF_SECURITY_COOKIE_SECURE: "true"
      GF_SECURITY_COOKIE_SAMESITE: "lax"
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_USERS_ALLOW_ORG_CREATE: "false"
    volumes:
      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards
      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources
      - grafana_staging_data:/var/lib/grafana
    ports:
      - "3001:3000"
    depends_on:
      - prometheus
      - loki
      - tempo
    networks:
      - soluciona-staging-network

  loki:
    image: grafana/loki:2.9.0
    container_name: soluciona-loki-staging
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - ./monitoring/loki-config.yaml:/etc/loki/local-config.yaml
      - loki_staging_data:/loki
    ports:
      - "3100:3100"
    networks:
      - soluciona-staging-network

  tempo:
    image: grafana/tempo:2.3.0
    container_name: soluciona-tempo-staging
    command: -config.file=/etc/tempo.yaml
    volumes:
      - ./monitoring/tempo.yaml:/etc/tempo.yaml
      - tempo_staging_data:/tmp/tempo
    ports:
      - "3200:3200"
      - "4317:4317"
      - "4318:4318"
    networks:
      - soluciona-staging-network

  alertmanager:
    image: prom/alertmanager:v0.26.0
    container_name: soluciona-alertmanager-staging
    volumes:
      - ./monitoring/alertmanager.yml:/etc/alertmanager/alertmanager.yml
      - alertmanager_staging_data:/alertmanager
    ports:
      - "9093:9093"
    networks:
      - soluciona-staging-network

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.0
    container_name: soluciona-cadvisor-staging
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    ports:
      - "8081:8080"
    networks:
      - soluciona-staging-network

volumes:
  postgres_staging_data:
  redis_staging_data:
  minio_staging_data:
  jaeger_staging_data:
  prometheus_staging_data:
  grafana_staging_data:
  loki_staging_data:
  tempo_staging_data:
  alertmanager_staging_data:

networks:
  soluciona-staging-network:
    driver: bridge